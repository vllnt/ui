---
title: "View Switcher"
description: "URL param-based toggle between named views with pill/tab styling. Install View Switcher from the VLLNT UI registry with the shadcn CLI."
keywords:
  - "view-switcher"
  - "view-switcher component"
  - "react view-switcher"
  - "shadcn view-switcher"
  - "navigation component"
---
URL param-based toggle between named views with pill/tab styling. Part of the navigation family in VLLNT UI, it ships as a
machine-readable registry entry — copy the source directly into your app with
the shadcn CLI and own it, no runtime dependency on a component library.

<Preview />

## Installation

Add View Switcher to your project with the shadcn CLI. The source lands in your
codebase, ready to adapt:

<Install />

## Source

<Code />

## Stories

Explore every variant and state in the interactive Storybook:

<Stories />
