---
title: "Jarvis Dock"
description: "Floating bottom dock with quick-action buttons + a command-palette trigger. Install Jarvis Dock from the VLLNT UI registry with the shadcn CLI."
keywords:
  - "jarvis-dock"
  - "jarvis-dock component"
  - "react jarvis-dock"
  - "shadcn jarvis-dock"
  - "navigation component"
---
Floating bottom dock with quick-action buttons + a command-palette trigger. Part of the navigation family in VLLNT UI, it ships as a
machine-readable registry entry — copy the source directly into your app with
the shadcn CLI and own it, no runtime dependency on a component library.

<Preview />

## Installation

Add Jarvis Dock to your project with the shadcn CLI. The source lands in your
codebase, ready to adapt:

<Install />

## Source

<Code />

## Stories

Explore every variant and state in the interactive Storybook:

<Stories />
