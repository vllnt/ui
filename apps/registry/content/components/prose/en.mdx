---
title: "Prose"
description: "Long-form content wrapper that styles raw HTML descendants with the token-driven type scale. Install Prose from the VLLNT UI registry with the shadcn CLI."
keywords:
  - "prose"
  - "prose component"
  - "react prose"
  - "shadcn prose"
  - "core component"
---
Long-form content wrapper that styles raw HTML descendants with the token-driven type scale. Part of the core family in VLLNT UI, it ships as a
machine-readable registry entry — copy the source directly into your app with
the shadcn CLI and own it, no runtime dependency on a component library.

<Preview />

## Installation

Add Prose to your project with the shadcn CLI. The source lands in your
codebase, ready to adapt:

<Install />

## Source

<Code />

## Stories

Explore every variant and state in the interactive Storybook:

<Stories />
