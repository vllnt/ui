---
title: "Playback Ghost"
description: "Translucent overlay marking where a canvas object was at a previous timestamp during playback. Install Playback Ghost from the VLLNT UI registry with the shadcn CLI."
keywords:
  - "playback-ghost"
  - "playback-ghost component"
  - "react playback-ghost"
  - "shadcn playback-ghost"
  - "utility component"
---
Translucent overlay marking where a canvas object was at a previous timestamp during playback. Part of the utility family in VLLNT UI, it ships as a
machine-readable registry entry — copy the source directly into your app with
the shadcn CLI and own it, no runtime dependency on a component library.

<Preview />

## Installation

Add Playback Ghost to your project with the shadcn CLI. The source lands in your
codebase, ready to adapt:

<Install />

## Source

<Code />

## Stories

Explore every variant and state in the interactive Storybook:

<Stories />
