---
title: "Theme Switcher"
description: "Compact swatch row for switching between built-in theme presets, kept in sync with every other consumer on the page. Install Theme Switcher from the VLLNT UI registry with the shadcn CLI."
keywords:
  - "theme-switcher"
  - "theme-switcher component"
  - "react theme-switcher"
  - "shadcn theme-switcher"
  - "utility component"
---
Compact swatch row for switching between built-in theme presets, kept in sync with every other consumer on the page. Part of the utility family in VLLNT UI, it ships as a
machine-readable registry entry — copy the source directly into your app with
the shadcn CLI and own it, no runtime dependency on a component library.

<Preview />

## Installation

Add Theme Switcher to your project with the shadcn CLI. The source lands in your
codebase, ready to adapt:

<Install />

## Source

<Code />

## Stories

Explore every variant and state in the interactive Storybook:

<Stories />
