---
title: "Input OTP"
description: "One-time password input with segmented fields. Install Input OTP from the VLLNT UI registry with the shadcn CLI."
keywords:
  - "input-otp"
  - "input-otp component"
  - "react input-otp"
  - "shadcn input-otp"
  - "form component"
---
One-time password input with segmented fields. Part of the form family in VLLNT UI, it ships as a
machine-readable registry entry — copy the source directly into your app with
the shadcn CLI and own it, no runtime dependency on a component library.

<Preview />

## Installation

Add Input OTP to your project with the shadcn CLI. The source lands in your
codebase, ready to adapt:

<Install />

## Source

<Code />

## Stories

Explore every variant and state in the interactive Storybook:

<Stories />
