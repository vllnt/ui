---
title: "Viewport Bookmarks"
description: "Saved-view list for the canvas — pinned spatial locations with optional active state. Install Viewport Bookmarks from the VLLNT UI registry with the shadcn CLI."
keywords:
  - "viewport-bookmarks"
  - "viewport-bookmarks component"
  - "react viewport-bookmarks"
  - "shadcn viewport-bookmarks"
  - "navigation component"
---
Saved-view list for the canvas — pinned spatial locations with optional active state. Part of the navigation family in VLLNT UI, it ships as a
machine-readable registry entry — copy the source directly into your app with
the shadcn CLI and own it, no runtime dependency on a component library.

<Preview />

## Installation

Add Viewport Bookmarks to your project with the shadcn CLI. The source lands in your
codebase, ready to adapt:

<Install />

## Source

<Code />

## Stories

Explore every variant and state in the interactive Storybook:

<Stories />
