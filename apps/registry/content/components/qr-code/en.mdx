---
title: "QR Code"
description: "Renders a QR code from a string value as a theme-aware SVG. Install QR Code from the VLLNT UI registry with the shadcn CLI."
keywords:
  - "qr-code"
  - "qr-code component"
  - "react qr-code"
  - "shadcn qr-code"
  - "core component"
---
Renders a QR code from a string value as a theme-aware SVG. Part of the core family in VLLNT UI, it ships as a
machine-readable registry entry — copy the source directly into your app with
the shadcn CLI and own it, no runtime dependency on a component library.

<Preview />

## Installation

Add QR Code to your project with the shadcn CLI. The source lands in your
codebase, ready to adapt:

<Install />

## Source

<Code />

## Stories

Explore every variant and state in the interactive Storybook:

<Stories />
