---
title: "Canvas Shell"
description: "Layout shell for canvas workspaces with top bar, left rail, right dock, and bottom slot regions. Install Canvas Shell from the VLLNT UI registry with the shadcn CLI."
keywords:
  - "canvas-shell"
  - "canvas-shell component"
  - "react canvas-shell"
  - "shadcn canvas-shell"
  - "utility component"
---
Layout shell for canvas workspaces with top bar, left rail, right dock, and bottom slot regions. Part of the utility family in VLLNT UI, it ships as a
machine-readable registry entry — copy the source directly into your app with
the shadcn CLI and own it, no runtime dependency on a component library.

<Preview />

## Installation

Add Canvas Shell to your project with the shadcn CLI. The source lands in your
codebase, ready to adapt:

<Install />

## Source

<Code />

## Stories

Explore every variant and state in the interactive Storybook:

<Stories />
