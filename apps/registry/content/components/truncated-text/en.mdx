---
title: "Truncated Text"
description: "Single-line text that truncates with an ellipsis when it exceeds a configurable max width. Install Truncated Text from the VLLNT UI registry with the shadcn CLI."
keywords:
  - "truncated-text"
  - "truncated-text component"
  - "react truncated-text"
  - "shadcn truncated-text"
  - "utility component"
---
Single-line text that truncates with an ellipsis when it exceeds a configurable max width. Part of the utility family in VLLNT UI, it ships as a
machine-readable registry entry — copy the source directly into your app with
the shadcn CLI and own it, no runtime dependency on a component library.

<Preview />

## Installation

Add Truncated Text to your project with the shadcn CLI. The source lands in your
codebase, ready to adapt:

<Install />

## Source

<Code />

## Stories

Explore every variant and state in the interactive Storybook:

<Stories />
