---
title: "Auto Reload"
description: "Toggle + collapsible threshold/amount form for automatic credit reloading with locale-aware currency. Install Auto Reload from the VLLNT UI registry with the shadcn CLI."
keywords:
  - "auto-reload"
  - "auto-reload component"
  - "react auto-reload"
  - "shadcn auto-reload"
  - "billing component"
---
Toggle + collapsible threshold/amount form for automatic credit reloading with locale-aware currency. Part of the billing family in VLLNT UI, it ships as a
machine-readable registry entry — copy the source directly into your app with
the shadcn CLI and own it, no runtime dependency on a component library.

<Preview />

## Installation

Add Auto Reload to your project with the shadcn CLI. The source lands in your
codebase, ready to adapt:

<Install />

## Source

<Code />

## Stories

Explore every variant and state in the interactive Storybook:

<Stories />
