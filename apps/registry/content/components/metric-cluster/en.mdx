---
title: "Metric Cluster"
description: "Compact stack of related metrics pinned to a canvas object's corner. Install Metric Cluster from the VLLNT UI registry with the shadcn CLI."
keywords:
  - "metric-cluster"
  - "metric-cluster component"
  - "react metric-cluster"
  - "shadcn metric-cluster"
  - "data component"
---
Compact stack of related metrics pinned to a canvas object's corner. Part of the data family in VLLNT UI, it ships as a
machine-readable registry entry — copy the source directly into your app with
the shadcn CLI and own it, no runtime dependency on a component library.

<Preview />

## Installation

Add Metric Cluster to your project with the shadcn CLI. The source lands in your
codebase, ready to adapt:

<Install />

## Source

<Code />

## Stories

Explore every variant and state in the interactive Storybook:

<Stories />
