{
  "a11y": {
    "notes": "Renders a semantic element (p, span, div, or label) chosen by `as`, and inherits the design-system foreground contrast tokens. For a real form label use the Label component, which wires the htmlFor association."
  }
}
